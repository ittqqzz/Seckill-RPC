<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 配置mybatis全局属性 -->
    <settings>
        <!-- 使用jdbc的getGeneratedKeys获取数据库自增主键值 -->
        <setting name="useGeneratedKeys" value="true" />

        <!-- 使用列别名替换列名 默认:true -->
        <setting name="useColumnLabel" value="true" />

        <!-- 开启驼峰命名转换:Table{create_time} -> Entity{createTime} -->
        <setting name="mapUnderscoreToCamelCase" value="true" />

        <setting name="logImpl" value="STDOUT_LOGGING" />
    </settings>

    <typeAliases>
        <typeAlias type="com.tqz.entity.User" alias="User"/>
        <typeAlias type="com.tqz.entity.SuccessKilled" alias="SuccessKilled"/>
        <typeAlias type="com.tqz.entity.Seckill" alias="Seckill"/>
    </typeAliases>

    <!--<mappers>-->
        <!--<mapper resource="classpath:mapper/AreaDao.xml"/>-->
    <!--</mappers>-->

</configuration>